# 2026-06-10 工作日志

## 燃气险原始码恢复（重点）

- **问题**：企业微信发文件收回来是 965B 的 COS 错误 JSON（NoSuchKey），原始 HTML 源码从未传到服务器
- **解决**：用户直接**粘贴源码文本**到对话框（不是发文件），我保存为 HTML 文件
- **收到原始码**：
  - `cp.html` — 产品介绍（25.8KB），含 ABCD 款对比表、6 道交互问答
  - `kh.html` — 销售话术（23.0KB），含一般款/特别款话术、13 个常见问题、5 道模拟练习
  - `al.html` — 理赔案例（20.2KB），含 4 个真实案例、4 题看图识灾
- **音频**：三个页面均调用 COS 桶 `naidou-audio99-1434426321` 的音频文件
- **本地服务**：pm2 守护 serve 进程，端口 8080
  - 之前 serve 进程没在 pm2 里（重启服务就没了）
  - 现在已加 pm2 守护：`pm2 start "npx serve ... --name naidu-serve"`

## 模型切换

- **Agnes 2.0 Flash → DeepSeek V4 Flash**
  - Agnes 频繁 503 不可用，导致企业微信收不到回复
  - 通过 `config.patch` 修改 `agents.defaults.model.primary` 切换
  - 热加载生效，gateway 重启确认

## 记忆系统

- **Meilisearch**：Docker 运行，端口 7700，索引 10 个记忆文件
- **COS 冷备**：~/.cos.conf 配置，coscmd 定期同步 memory/ 目录
- **MEMORY.md** 已更新，记录最终状态

## 企业微信问题排查

- **原因**：Agnes 503 不可用 → 模型回复超时 → 消息发不出去
- **不是企业微信本身的问题**
- 文件传输管道 bug 已确认：企业微信传文件会返回 COS 错误 JSON

## 23:56 - 用户睡觉
- 默认模型已改回 DeepSeek V4 Flash
- 企业微信应恢复正常（不再走 Agnes）

## 待办

- [ ] 用户验证三个页面在本地和 COS 上是否正常显示
- [ ] 确认 COS 上三个 HTML 文件是否同步更新（当前只在本地）
- [ ] 确认 cp.html 内容是否完整（之前保存疑似截断）
